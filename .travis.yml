language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-css-testing' ]; then cd .. && eval "mv $currentfolder generator-css-testing" && cd generator-css-testing; fi

